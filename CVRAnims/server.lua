----------------------------------------Spoilers-----------------------------------
addEvent("upgradePro", true)
addEventHandler("upgradePro", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1000 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeWin", true)
addEventHandler("upgradeWin", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1001 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeDrag", true)
addEventHandler("upgradeDrag", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1002 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeAlpha", true)
addEventHandler("upgradeAlpha", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1003 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeChamp", true)
addEventHandler("upgradeChamp", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1014 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeRace", true)
addEventHandler("upgradeRace", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1015 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeWorx", true)
addEventHandler("upgradeWorx", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1016 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeFury", true)
addEventHandler("upgradeFury", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1023 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeAlien", true)
addEventHandler("upgradeAlien", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1049 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeXFlow", true)
addEventHandler("upgradeXFlow", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1050 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeAlien2", true)
addEventHandler("upgradeAlien2", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1058 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeXFlow2", true)
addEventHandler("upgradeXFlow2", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1060 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeAlien3", true)
addEventHandler("upgradeAlien3", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1138 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeXFlow3", true)
addEventHandler("upgradeXFlow3", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1139 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeXFlow4", true)
addEventHandler("upgradeXFlow4", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1146 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeAlien4", true)
addEventHandler("upgradeAlien4", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1147 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeXFlow5", true)
addEventHandler("upgradeXFlow5", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1158 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeAlien5", true)
addEventHandler("upgradeAlien5", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1162 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeXFlow6", true)
addEventHandler("upgradeXFlow6", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1163 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("upgradeAlien6", true)
addEventHandler("upgradeAlien6", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1164 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
-------------------------------------------------------------------------------------------------------------
----------------------------------------Hydraulics-----------------------------------
addEvent("upgradeHydraulics", true)
addEventHandler("upgradeHydraulics", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(750)) then
            takePlayerMoney (source, tonumber (750))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1087 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
-------------------------------------------------------------------------------------------------------------
----------------------------------------Nitro-----------------------------------
addEvent("addNitrox2", true)
addEventHandler("addNitrox2", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1009 )
            outputChatBox ( "You're vehicle is succefully upgraded. Nitro x2", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
addEvent("addNitrox5", true)
addEventHandler("addNitrox5", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1000)) then
            takePlayerMoney (source, tonumber (1000))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1008 )
            outputChatBox ( "You're vehicle is succefully upgraded. Nitro x5", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
addEvent("addNitrox10", true)
addEventHandler("addNitrox10", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(5000)) then
            takePlayerMoney (source, tonumber (5000))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1010 )
            outputChatBox ( "You're vehicle is succefully upgraded. Nitro x10", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
-------------------------------------------------------------------------------------------------------------
----------------------------------------Paintjobs-----------------------------------
addEvent("addPaintjob1", true)
addEventHandler("addPaintjob1", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            setVehiclePaintjob ( theVehicle, 0 )   
            outputChatBox ( "You're vehicle is succefully upgraded. Paintjob 1", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
addEvent("addPaintjob2", true)
addEventHandler("addPaintjob2", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1000)) then
            takePlayerMoney (source, tonumber (1000))
            local theVehicle = getPedOccupiedVehicle (source)
            setVehiclePaintjob ( theVehicle, 1 )   
            outputChatBox ( "You're vehicle is succefully upgraded. Paintjob 2", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
addEvent("addPaintjob3", true)
addEventHandler("addPaintjob3", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(5000)) then
            takePlayerMoney (source, tonumber (5000))
            local theVehicle = getPedOccupiedVehicle (source)
            setVehiclePaintjob ( theVehicle, 2 )   
            outputChatBox ( "You're vehicle is succefully upgraded. Paintjob 3", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
-------------------------------------------------------------------------------------------------------------
----------------------------------------Exhausts-----------------------------------
addEvent("addUpswept", true)
addEventHandler("addUpswept", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1018 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addTwin", true)
addEventHandler("addTwin", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1019 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addLarge", true)
addEventHandler("addLarge", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1020 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addMedium", true)
addEventHandler("addMedium", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1021 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addSmall", true)
addEventHandler("addSmall", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1022 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addXFlow", true)
addEventHandler("addXFlow", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1036 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addAlien", true)
addEventHandler("addAlien", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1034 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addSlaminExhaust", true)
addEventHandler("addSlaminExhaust", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1127 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addChromeExhaust", true)
addEventHandler("addChromeExhaust", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1126 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addSlamin", true)
addEventHandler("addSlamin", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1089 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addChrome", true)
addEventHandler("addChrome", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (1500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1092 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
---------------------------------------------------------------------------------------------------------------
----------------------------------------Wheels-----------------------------------
addEvent("addOffroad", true)
addEventHandler("addOffroad", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1025 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addShadow", true)
addEventHandler("addShadow", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1073 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addMega", true)
addEventHandler("addMega", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1074 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addRimshine", true)
addEventHandler("addRimshine", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1075 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addWires", true)
addEventHandler("addWires", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1076 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addClassic", true)
addEventHandler("addClassic", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1077 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addTwist", true)
addEventHandler("addTwist", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1078 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addCutter", true)
addEventHandler("addCutter", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1079 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addSwitch", true)
addEventHandler("addSwitch", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1080 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addGrove", true)
addEventHandler("addGrove", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1081 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addImport", true)
addEventHandler("addImport", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1082 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addDollar", true)
addEventHandler("addDollar", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1083 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addTrance", true)
addEventHandler("addTrance", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1084 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addAtomic", true)
addEventHandler("addAtomic", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1085 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addAhab", true)
addEventHandler("addAhab", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1096 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addVirtual", true)
addEventHandler("addVirtual", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1097 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)

addEvent("addAccess", true)
addEventHandler("addAccess", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(1500)) then
            takePlayerMoney (source, tonumber (2500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1098 )
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
---------------------------------------------------------------------------------------------------------------
----------------------------------------Hoods-----------------------------------
--[[addEvent("champScoop", true)
addEventHandler("champScoop", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1004 ) 
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
addEvent("furyScoop", true)
addEventHandler("furyScoop", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1005 ) 
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
addEvent("raceScoop", true)
addEventHandler("raceScoop", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1011 )
            outputChatBox ( "You're vehicle is succefully upgraded. ", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)
addEvent("worxScoop", true)
addEventHandler("worxScoop", root,
function ()
        if isPedInVehicle (source) and (getPlayerMoney (source) >= tonumber(500)) then
            takePlayerMoney (source, tonumber (500))
            local theVehicle = getPedOccupiedVehicle (source)
            addVehicleUpgrade ( theVehicle, 1012 ) 
            outputChatBox ( "You're vehicle is succefully upgraded.", source, 250, 250, 0 )
        else
            outputChatBox ( "You must be in a vehicle!", source, 250, 0, 0 )
        end
end)]]
-------------------------------------------------------------------------------------------------------------
